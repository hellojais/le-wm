dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 1
  name: billiards_expert_train
  keys_to_load:
    - pixels
    - action
    - state
  keys_to_cache:
    - action
    - state
